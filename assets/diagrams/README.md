# Diagrams

Architecture and flow diagrams are authored as **Mermaid** and embedded **inline**
in the documentation (see `docs/`), so they render directly on GitHub and stay in
version control as text.

Standalone diagram sources (`.mmd`) or exported images may be added here later. Any
image added must contain **no** personal data, real client identifiers, or secrets.
